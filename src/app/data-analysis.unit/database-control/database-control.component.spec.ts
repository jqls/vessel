/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DatabaseControlComponent } from './database-control.component';

describe('Component: DatabaseControl', () => {
  it('should create an instance', () => {
    let component = new DatabaseControlComponent();
    expect(component).toBeTruthy();
  });
});
